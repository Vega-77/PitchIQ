// Where the backend is running. On match day this is the laptop's LAN address
// (find it with `ipconfig`), so the tablet can reach it over shared WiFi.
// Edit by hand — there is no build step.
const API_BASE = 'http://127.0.0.1:8000';
