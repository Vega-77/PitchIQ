# PitchIQ Python dependencies (cv/ and PitchIQHelper/ share one venv).
#
# GPU note: `pip install -r requirements.txt` alone installs the CPU-only build of
# torch. For CUDA, install torch/torchvision first from the matching PyTorch index,
# then install the rest:
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
#   pip install -r requirements.txt
#
# Pick the cuXXX tag matching your driver (`nvidia-smi`); cu130 is what this project
# was developed against on an RTX 4060.

# --- computer vision (cv/) ---
opencv-python==5.0.0.93
ultralytics==8.4.108
torch==2.13.0
torchvision==0.28.0
numpy==2.4.4
# Runs the trained xG model from Python, so cv/xg_bridge.py can be checked
# against the copy the browser loads. The browser uses onnxruntime-web instead.
onnxruntime==1.28.0

# The app itself needs no Python at all. Data and auth are Firestore, the
# frontend is plain ES modules on GitHub Pages, and the only Python left is the
# offline CV work above. A FastAPI + SQLAlchemy + SQLite server used to live in
# backend/; Firestore replaced it, and it was deleted once a full match had been
# tagged without it.

# --- xG model training (PitchIQHelper/main.py) ---
# Not needed to run the app; only to retrain the xG model from StatsBomb data.
# statsbombpy
# xgboost
# scikit-learn
# pandas
# matplotlib
# onnxmltools
